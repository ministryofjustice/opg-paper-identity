{
  "name": "e2e-tests",
  "version": "1.0.0",
  "scripts": {
    "lint": "npx eslint",
    "test": "cypress open",
    "test-headless": "cypress run --headless -b electron"
  },
  "devDependencies": {
    "cypress": "^14.0.0",
    "typescript": "*",
    "typescript-eslint": "^8.0.1"
  }
}
