<div class="govuk-width-container">
    <main class="govuk-main-wrapper">
        <h1 class="govuk-heading-xl">What are the first two letters of the last name of another person on the electroal register at your address?</h1>
        <div class="govuk-form-group">
            <fieldset class="govuk-fieldset">
                <legend class="govuk-fieldset__legend govuk-fieldset__legend--l">
                    <h2 class="govuk-fieldset__heading">
                        Select answer
                    </h2>
                </legend>
                <div class="govuk-radios" data-module="govuk-radios">
                    {% for datum in initials_electoral_register_data %}
                        <div class="govuk-radios__item">
                            <input class="govuk-radios__input" id="{{ datum }}" name="mortgage_provider" type="radio" value="{{ datum }}">
                            <label class="govuk-label govuk-radios__label" for="whereDoYouLive">{{ datum }}</label>
                        </div>
                    {% endfor %}
                </div>
            </fieldset>
            <input type="hidden" name="question4">
        </div>
        <button class="govuk-button">Continue</button>
    </main>
</div>
